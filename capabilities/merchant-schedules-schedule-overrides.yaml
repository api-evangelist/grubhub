naftiko: 1.0.0-alpha2
info:
  label: Grubhub Merchant Schedules API — Schedule Overrides
  description: 'Grubhub Merchant Schedules API — Schedule Overrides. 2 operations. Lead operation: Create a schedule override.
    Self-contained Naftiko capability covering one Grubhub business surface.'
  tags:
  - Grubhub
  - Schedule Overrides
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GRUBHUB_API_KEY: GRUBHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: merchant-schedules-schedule-overrides
    baseUri: https://api-third-party-gtm.grubhub.com
    description: Grubhub Merchant Schedules API — Schedule Overrides business capability. Self-contained, no shared references.
    resources:
    - name: pos-v1-merchant-merchant_id-schedules-overrides
      path: /pos/v1/merchant/{merchant_id}/schedules/overrides
      operations:
      - name: createscheduleoverride
        method: POST
        description: Create a schedule override
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: cancelscheduleoverride
        method: DELETE
        description: Cancel a schedule override
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: apikey
      key: Authorization
      value: '{{env.GRUBHUB_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: merchant-schedules-schedule-overrides-rest
    port: 8080
    description: REST adapter for Grubhub Merchant Schedules API — Schedule Overrides. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/pos/v1/merchant/{merchant-id}/schedules/overrides
      name: pos-v1-merchant-merchant-id-schedules-overrides
      description: REST surface for pos-v1-merchant-merchant_id-schedules-overrides.
      operations:
      - method: POST
        name: createscheduleoverride
        description: Create a schedule override
        call: merchant-schedules-schedule-overrides.createscheduleoverride
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: cancelscheduleoverride
        description: Cancel a schedule override
        call: merchant-schedules-schedule-overrides.cancelscheduleoverride
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: merchant-schedules-schedule-overrides-mcp
    port: 9090
    transport: http
    description: MCP adapter for Grubhub Merchant Schedules API — Schedule Overrides. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: create-schedule-override
      description: Create a schedule override
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: merchant-schedules-schedule-overrides.createscheduleoverride
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: cancel-schedule-override
      description: Cancel a schedule override
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: merchant-schedules-schedule-overrides.cancelscheduleoverride
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
