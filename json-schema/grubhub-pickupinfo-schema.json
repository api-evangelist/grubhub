{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/api-evangelist/grubhub/main/json-schema/grubhub-pickupinfo-schema.json",
  "title": "PickupInfo",
  "x-generated": "2026-09-17",
  "x-method": "derived",
  "x-source": "openapi/grubhub-orders-openapi.yml#/components/schemas/PickupInfo",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the person coming to pickup the order"
    },
    "contact_info": {
      "$ref": "#/$defs/ContactInfo"
    },
    "instructions": {
      "type": "string",
      "description": "Special instructions for pickup"
    },
    "estimated_pickup_time": {
      "$ref": "#/$defs/Instant"
    },
    "is_green_indicated": {
      "type": "boolean",
      "description": "if set to true, the merchant should not include napkins, utensils and other items that would just become waste as the diner has access to their own dishes and utensils"
    }
  },
  "description": "Information about the pickup order",
  "$defs": {
    "Chronology": {
      "type": "object",
      "properties": {
        "zone": {
          "$ref": "#/$defs/DateTimeZone"
        }
      }
    },
    "ContactInfo": {
      "type": "object",
      "properties": {
        "phone": {
          "type": "string",
          "description": "Phone number of the diner. This number can only be used for the fulfillment of the order and may not be stored and used for any sort of marketing, research or other contacts not directly related to the delivery or pickup of this specific order."
        },
        "name": {
          "type": "string",
          "description": "Name of the diner"
        },
        "email": {
          "type": "string",
          "description": "E-mail address of the diner"
        }
      },
      "description": "Basic diner contact information"
    },
    "DateTimeZone": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string"
        },
        "fixed": {
          "type": "boolean"
        }
      },
      "description": "The timezone of the restaurant"
    },
    "Instant": {
      "type": "object",
      "properties": {
        "chronology": {
          "$ref": "#/$defs/Chronology"
        },
        "millis": {
          "type": "integer",
          "format": "int64"
        },
        "zone": {
          "$ref": "#/$defs/DateTimeZone"
        },
        "after_now": {
          "type": "boolean"
        },
        "before_now": {
          "type": "boolean"
        },
        "equal_now": {
          "type": "boolean"
        }
      },
      "description": "The time that the diner placed the order"
    }
  }
}