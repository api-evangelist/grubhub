{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/api-evangelist/grubhub/main/json-schema/grubhub-progressdeliveryrequest-schema.json",
  "title": "ProgressDeliveryRequest",
  "x-generated": "2026-09-17",
  "x-method": "derived",
  "x-source": "openapi/grubhub-connect-endpoints-openapi.yml#/components/schemas/ProgressDeliveryRequest",
  "required": [
    "delivery_state"
  ],
  "type": "object",
  "properties": {
    "delivery_state": {
      "type": "string",
      "description": "The subset of delivery states that a partner can progress a delivery to.",
      "enum": [
        "ASSIGNED",
        "UNASSIGNED",
        "COURIER_AT_PICKUP",
        "PICKED_UP",
        "IN_TRANSIT",
        "COURIER_AT_DROPOFF",
        "DELIVERED",
        "CANCELED"
      ]
    },
    "speed_mph": {
      "minimum": 0,
      "type": "integer",
      "description": "The speed (in mph) at which the courier should move to the given delivery state. If no value is provided, the default speed of 30 mph will be used.",
      "format": "int32",
      "example": 30,
      "default": 30
    }
  },
  "description": "The request information for progressing a test delivery."
}